{
  "name": "small-libs",
  "version": "0.0.4",
  "files": ["dist"],
  "bin": {
    "small-libs": "./dist/index.js"
  },
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "build": "bun build --minify --target bun --outdir dist index.ts && chmod +x dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
